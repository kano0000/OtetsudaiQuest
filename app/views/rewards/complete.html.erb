<div class="complete_bg">
  <div class="container mt-3">
    <div class="row">
      <div class="col-10 mx-auto">
        <h2 class= "ribon"><span><%= @reward.name %> ゲット！！</span></h2>
        <div class="text-white">
          <h4><ruby>交換<rt>こうかん</rt></ruby>したよ！これからもおてつだいがんばってね</h4>
          <div class="brown-box col-7">
            <div class="side_image text-left">
              <%= image_tag @child.get_profile_image(200,200) %>
            </div>
            <div class="side_text">
              <p><%= I18n.t("enums.child.level.#{@level}") %> のおてつだいヒーロー</p>
              <h3><%= @child.name%></h3>
              <% if request.post? %>
                <% new_point = @child.point - @reward.point %>
                <% @child.update(point: new_point) %>
              <% end %>
              <p>のこり <%= request.post? ? new_point : @child.point %> ポイント</p>
              <%= link_to child_path(@child), class:"btn btn-danger rounded-pill", :style => "text-decoration: none; font-weight:bold; color:#fff;" do %>
                <i class="fa-solid fa-user"></i>マイページをみる
              <% end %>
            </div>
          </div>
        </div>
         <div>
          <%= image_tag "complete.png", class: "img-fluid treasure" %>
        </div>
      </div>
    </div>
  </div>
</div>