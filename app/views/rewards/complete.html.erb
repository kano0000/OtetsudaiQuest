<div class="complete_bg">
  <div class="container mt-3">
    <div class="row">
      <div class="col-10 mx-auto">
        <h2 class= "ribon"><span><%= @reward.name %> <br class="sp_br">ゲット！！</span></h2>
        <div class="text-white">
          <h4><ruby>交換<rt>こうかん</rt></ruby>したよ！<br class="sp_br">これからもおてつだいがんばってね</h4>
          <div class="neon-box3 col-10 mx-auto my-5">
            <div class="row">
              <div class="col-md-6">
                <%= image_tag @child.get_profile_image(180,180) %>
              </div>
              <div class="col-md-6">
                <p><%= I18n.t("enums.child.level.#{@level}") %> のおてつだいヒーロー</p>
                <div class="row mt-3">
                  <div class="col-12">
                    <h3><%= @child.name%></h3>
                  </div>
                </div>
                <div class="row mt-3">
                  <div class="col-12">
                    <% if request.post? %>
                      <% new_point = @child.point - @reward.point %>
                      <% @child.update(point: new_point) %>
                    <% end %>
                    <!--postかgetを判別する。（リロードでポイントが減らないようにする）-->
                    <p>のこり <%= request.post? ? new_point : @child.point %> ポイント</p>
                  </div>
                </div>
                <div class="row mt-3">
                  <div class="col-12">
                    <%= link_to child_path(@child), class:"btn btn-danger rounded-pill", :style => "text-decoration: none; font-weight:bold; color:#fff;" do %>
                      <i class="fa-solid fa-user"></i>マイページ
                    <% end %>
                  </div>
                </div>
              </div>
            </div>
          </div>
          <div>
            <%= image_tag "complete.png", class: "img-fluid treasure" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>